'use client';

import { formatCurrency } from '../utils/formatters';

export default function CashFlowOverview({ data }) {
  // Implementation will be moved from original page.js
  // For now, returning a placeholder component
  return (
    <div className="space-y-6">
      <h2 className="text-xl font-semibold text-gray-900 mb-4">
        Cashflow Overzicht
      </h2>
      <div className="bg-white border border-gray-200 rounded-lg p-6">
        <p className="text-gray-600">
          Cashflow overzicht wordt binnenkort verplaatst naar deze component.
        </p>
      </div>
    </div>
  );
}
